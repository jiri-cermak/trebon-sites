# Shared Dockerfile template for all trebon-sites
# Copy to each site directory and adjust COPY paths as needed.
FROM nginx:alpine
COPY nginx.conf /etc/nginx/nginx.conf
COPY src/ /usr/share/nginx/html/
EXPOSE 80
